default:
  suites:
    insert:
      paths:
        - '%paths.base%/features/insert.feature'
      contexts:
        - InsertContext
    edit:
      paths:
        - '%paths.base%/features/edit.feature'
      contexts:
        - EditContext
    bookmark:
      paths:
        - '%paths.base%/features/bookmark.feature'
      contexts:
        - BookmarkContext 
    summary:
      paths:
        - '%paths.base%/features/summary.feature'
      contexts:
        - SummaryContext
    time_filter:
      paths:
        - '%paths.base%/features/time_filter.feature'
      contexts:
        - TimeFilterContext

